{
  "name": "dawn",
  "version": "1.0.0",
  "description": "The ultimate android frida-agent starting point",
  "private": true,
  "main": "agent/index.ts",
  "scripts": {
    "prepare": "npm run build",
    "build": "npm run c && frida-compile agent/index.ts -o _agent.js -c",
    "watch": "nodemon --watch agent/ -e 'c,h' -x \"npm run c\" & frida-compile agent/index.ts -o _agent.js -w",
    "c": "bash scripts/build.sh"
  },
  "devDependencies": {
    "@types/frida-gum": "^18.5.1",
    "@types/node": "^18.19.3",
    "nodemon": "^3.0.3",
    "frida-compile": "^16.4.1"
  }
}
